*.iml
.gradle
.kotlin
app/src/androidTest
app/src/test
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
# 构建产物：不限层级，覆盖 app/ hidden-api/ 等所有模块
build/
/captures
.externalNativeBuild
.cxx
local.properties

# 正式签名（私密，不要提交）
keystore.properties
*.jks

# IDE 本地配置（Android Studio 自动生成，不需要进仓库）
/.idea/

# 运行数据 / 私人文档（不需要进仓库）
stats.json
server/data
AI对话文档.md

# 备份压缩包（避免把整包误提交进仓库）
*.zip

# 后台看板本地预览脚本的数据缓存（运行时生成）
server/.preview-stats.json
